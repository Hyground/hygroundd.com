import Link from "next/link";
import { Header } from "@/components/layout/Header";

const tools = [["Card Manager", "Gestor/calculadora de tarjeta de crédito.", "/card", "ACTIVA"], ["Video Downloader", "Arquitectura inicial preparada.", "#", "PRÓXIMAMENTE"], ["PDF → Word", "Conversión de documentos.", "#", "PRÓXIMAMENTE"], ["Word → PDF", "Conversión de documentos.", "#", "PRÓXIMAMENTE"], ["Image Tools", "Compresión, resize y conversión.", "#", "PRÓXIMAMENTE"], ["QR Generator", "Generador de códigos QR.", "#", "PRÓXIMAMENTE"], ["Password Generator", "Generador seguro de contraseñas.", "#", "PRÓXIMAMENTE"], ["JSON Formatter", "Formatea y valida JSON.", "#", "PRÓXIMAMENTE"]] as const;

export default async function ToolsPage() { return <main className="hubShell"><Header /><section className="hubListing"><p className="hubEyebrow">01 / HERRAMIENTAS</p><h1>TOOLS <span>HUB</span></h1><p>Utilidades creadas para trabajo real. Las herramientas no disponibles no simulan funcionalidad.</p><div className="hubCards">{tools.map(([name, description, href, status], index) => href === "#" ? <article className="hubCard" key={name}><small>0{index + 1} / {status}</small><h3>{name}</h3><p>{description}</p></article> : <Link className="hubCard" href={href} key={name}><small>0{index + 1} / {status}</small><h3>{name}</h3><p>{description}</p><span>→</span></Link>)}</div></section></main>; }
